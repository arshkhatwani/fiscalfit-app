import { progressEmpty } from '../constants/colors';

const styles = {
  container: {
    overflow: 'hidden' as 'hidden',
    borderRadius: 100,
    height: 6,
    width: '100%',
    backgroundColor: progressEmpty,
  },
};

export default styles;
