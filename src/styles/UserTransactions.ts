import { darkHeading } from '../constants/colors';
import { font } from '../constants/fonts';

const styles = {
  heading: {
    color: darkHeading,
    fontFamily: font.bold.fontFamily,
    fontSize: 18,
  },
};

export default styles;
